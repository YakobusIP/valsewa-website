[Service]
# Clear the template ExecStart
ExecStart=

# Run the shared script with job-specific URL
ExecStart=/usr/bin/flock -n /run/lock/backend-cron-sync-expired.lock \
  /opt/schedulers/scheduler-post-request.sh \
  ${BACKEND_INTERNAL_URL}/api/bookings/sync-expired
