[Service]
# Clear the template ExecStart
ExecStart=

# Run the shared script with job-specific URL
ExecStart=/usr/bin/flock -n /run/lock/backend-cron-check-expiration.lock \
  /opt/schedulers/scheduler-post-request.sh \
  ${BACKEND_INTERNAL_URL}/api/vouchers/check-expiration
