[Service]
# Clear the template ExecStart
ExecStart=

# Run the shared script with job-specific URL
ExecStart=/usr/bin/flock -n /run/lock/backend-cron-update-rank.lock \
  /opt/schedulers/scheduler-post-request.sh \
  ${BACKEND_INTERNAL_URL}/api/accounts/update-rank