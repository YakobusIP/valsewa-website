[Service]
# Clear the template ExecStart
ExecStart=

# Run the shared script with job-specific URL
ExecStart=/usr/bin/flock -n /run/lock/backend-cron-check-password-expiration.lock \
  /opt/schedulers/scheduler-post-request.sh \
  http://127.0.0.1:5000/api/customer/check-password-expiration
